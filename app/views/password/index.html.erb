<div class="row">
  <%= form_for(:user, url: password_confirmation_url, method: :get) do |f| %>
  <a class="form-input"><%= f.text_field :email, placeholder: 'Email' %></a>
  <%= f.submit("Submit") %>
  <% end %>
</div>