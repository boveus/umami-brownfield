<div class="row">
  <div class="card grey darken-1 large">
    <div class="card-content white-text">
      <h5 class="center_text">Your User Profile</h5>
      <hr>
      <h3>Username: <%= @user.name %></h1>
      <h4>Email: <%= @user.email %></h4>
      <h4>Address: <%= @user.address %></h4>
      <hr>
      <div>
        <%= link_to "Edit Info", edit_user_path(@user), class: "waves-effect waves-light btn-large"  %>
        <%= link_to "Your Dashboard", dashboard_path(@user), class: "waves-effect waves-light btn-large"  %>
        <%= link_to "Orders", orders_path, class: "waves-effect waves-light btn-large" %>
        <%= link_to "Items", items_path, class: "waves-effect waves-light btn-large" %>
      </div>
    </div>
  </div>
</div>
